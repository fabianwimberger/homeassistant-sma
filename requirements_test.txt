# Test dependencies for Home Assistant integration
# Compatible with Home Assistant 2026.7.x

# Core testing
pytest==9.0.3
pytest-aiohttp==1.1.1
pytest-cov==7.1.0

# Home Assistant testing framework
# Pin to specific version matching HA core
pytest-homeassistant-custom-component==0.13.346

# HA core (for type checking and imports)
homeassistant==2026.7.2

# HTTP client (match manifest.json)
aiohttp==3.14.1

# HTTP mocking for tests
# aioresponses is unmaintained and incompatible with aiohttp>=3.14 (pnuckowski/aioresponses#288, unmerged);
# aioresponses-ng is a maintained fork with the same import path
aioresponses-ng==0.8.2

# Linting and formatting
ruff==0.15.20
mypy==2.1.0
